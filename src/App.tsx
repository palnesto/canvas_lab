import CanvasLabsLanding from "./CanvasLabsLanding";

/**
 * Active UI: Canvas Labs marketing landing (converted from static HTML).
 *
 * To restore the previous app UI:
 * 1. Comment out the `CanvasLabsLanding` import and the `App` function below.
 * 2. Uncomment the two lines at the bottom that re-export `LegacyApp`.
 *
 * The previous implementation lives in `LegacyApp.tsx` unchanged.
 */
export default function App() {
  return <CanvasLabsLanding />;
}

// import LegacyApp from "./LegacyApp";
// export default LegacyApp;
